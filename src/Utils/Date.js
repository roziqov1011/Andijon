// TODO Date function

// let apiDate = postData?.map((data) => {
    //     return data?.created_at.slice(0, 10)
    // })

    // const GetDatMonthFromDate = (dateSplit: any) => {
    //     dateSplit = dateSplit.split('-');
    //     var monthShortNames = ["Jan", "Feb", "Mar", "Apr", "May", "Jun",
    //         "Jul", "Aug", "Sep", "Oct", "Nov", "Dec"
    //     ];
    //     var months = ["January", "February", "March", "April", "May", "June", "July", "August", "September", "October", "November", "December"];

    //     const date = new Date(dateSplit[0], dateSplit[1], dateSplit[2]); // 2020-06-21
    //     return dateSplit[2] + ' ' + monthShortNames[date.getMonth()];
    // }

    // console.log(GetDatMonthFromDate())

    // const dateStr = apiDate[0];
    // const d = new Date(dateStr);
    // const result = new Intl.DateTimeFormat('default', { month: 'long' }).format(d);

    // console.log(result);